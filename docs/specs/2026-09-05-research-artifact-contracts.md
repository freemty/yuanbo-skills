# Research artifact contracts: result-driven iteration

Date: 2026-09-05. Base: outer `2c0005e`; isolated branch
`codex/research-artifact-contracts-20260905`. This is a follow-up to the
capability-first refactor, not a new aggregate skill or model migration.

## Scope and unchanged contracts

- Research Slides: retain the Speculative Decoding black Metropolis profile and
  mechanism-first teaching grammar; separate adjustable story budgets from evidence
  and visual completion boundaries. Cover empirical, theoretical, explanatory and
  proposal claims. Repair preserves the user's established visual system.
- Paper Storyteller: retain intellectual lineage and restrained personal taste;
  keep local edits local, analogy optional, and novelty bounded by actual evidence.
- Paper Style: preserve five palettes, Python API, licensed personal report class
  and legacy preamble entry. Existing venue injection must not change layout,
  typography, author masking, citations or vendor class files.
- Preserve host metadata, installation and current main checkout. No publishing,
  merge, installation or third-party `latex-document` changes in this iteration.

## Implementation and acceptance

1. Baseline independent skill-consuming tasks before instruction changes: five-minute
   toy queue explanation, midpoint theorem, adaptive draft proposal, local Methods edit.
2. Keep full references consistent with entry contracts. Counts are defaults; no
   fictitious result/ablation or required recurring metaphor.
3. Preflight all initializer files, including Python assets, before any mutation;
   preserve existing conflicting content and make repeated identical paper setup a no-op.
4. Build with available/project engine and converged references, reject known stale
   dependencies, render into unique directories, distinguish build from visual QA.
5. Actually compile and inspect PDFs. Test an authored venue stand-in before/after
   palette integration plus the legacy personal report. Do not call this official
   conference-template certification.
6. Run unchanged repository format/context/installer checks in the isolated worktree.
   Record failed attempts, fixes and the limits of single-run model evaluations.
7. Review and commit changed submodules before the outer gitlinks. No push/install.

Results and reproducible commands: `docs/reviews/2026-09-05-research-artifact-validation.md`.
