# research-slides

Research presentation workflow for restrained, source-backed Beamer decks.

Use when creating or revising talks from papers, blog posts, experiments, or literature notes.

## Install

Use the repo installer:

```bash
./install.sh --target codex
```

Manual Codex install:

```bash
ln -sf "$(pwd)" ~/.agents/skills/research-slides
```
