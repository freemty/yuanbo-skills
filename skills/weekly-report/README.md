# weekly-report

Manager-facing weekly progress report writer.

Use when preparing a weekly report across one or more repositories, with git history and project evidence folded into concise progress bullets.

## Install

Use the repo installer:

```bash
./install.sh --target codex
```

Manual Codex install:

```bash
ln -sf "$(pwd)" ~/.agents/skills/weekly-report
```
